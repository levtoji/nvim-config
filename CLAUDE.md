# nvim-config

Private dotfiles-Repo für die persönliche Neovim-Config.

## Branching-Ausnahme

Abweichend von der globalen Regel ("nie direkt auf main committen"): In
diesem Repo **immer direkt auf `main` committen und pushen**, kein
Feature-Branch, kein PR. Explizit vom Nutzer so gewünscht (2026-09-08).
